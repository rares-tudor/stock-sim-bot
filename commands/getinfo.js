module.exports = {
    name: 'getinfo',
    description: 'Allows users to get the info of a specific company. (Case Sensitive)', // Change the case sensitivity
    execute() {
        // Only used for command handling, can't move the command here because of DB implications
    }
}
 